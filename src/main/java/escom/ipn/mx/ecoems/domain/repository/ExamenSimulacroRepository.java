package escom.ipn.mx.ecoems.domain.repository;

import escom.ipn.mx.ecoems.domain.entity.ExamenSimulacro;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExamenSimulacroRepository extends JpaRepository<ExamenSimulacro, Long> {
}